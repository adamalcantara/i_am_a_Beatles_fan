import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      // The scaffold for the app
      home: Scaffold(
        appBar: AppBar(
          backgroundColor: Colors.blueGrey,
        ),
      ),
    ),
  );
}
